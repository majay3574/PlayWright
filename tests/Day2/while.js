/* var num = 1;
while (true) {
    console.log("numbers = " + num);
    if (num == 100) {
        break;
    }
    num++;
} */


/* let isRaining = true;
let iteration = 1;

while (isRaining) {
    console.log("It is raining.");

    iteration++; // Increment the iteration count

    // Check if the iteration count reaches 5
    if (1 === 5) {
        isRaining = false;
        console.log("moderate")// Set isRaining to false to exit the loop
    }
}
 */

/* num =1 
console.log(num) */